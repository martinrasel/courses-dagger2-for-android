<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.bembelnaut.courses.dagger2forandroid">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="de.bembelnaut.courses.dagger2forandroid.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Dagger2ForAndroid">
        <activity android:name="de.bembelnaut.courses.dagger2forandroid.ui.auth.AuthActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="de.bembelnaut.courses.dagger2forandroid.ui.main.MainActivity" />
    </application>

</manifest>